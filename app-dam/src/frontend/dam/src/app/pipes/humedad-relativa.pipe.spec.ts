import { HumedadRelativaPipe } from './humedad-relativa.pipe';

describe('HumedadRelativaPipe', () => {
  it('create an instance', () => {
    const pipe = new HumedadRelativaPipe();
    expect(pipe).toBeTruthy();
  });
});
