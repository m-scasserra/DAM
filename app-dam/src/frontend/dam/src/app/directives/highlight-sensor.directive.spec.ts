import { HighlightSensorDirective } from './highlight-sensor.directive';

describe('HighlightSensorDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightSensorDirective();
    expect(directive).toBeTruthy();
  });
});
